{
    "family": "course-management",
    "networkMode": "awsvpc",
    "executionRoleArn": "arn:aws:iam::585008058878:role/ecs-task-execution-role",
    "containerDefinitions": [
      {
        "name": "course-management",
        "image": "replace-this-in-workflow",
        "essential": true,
        "portMappings": [
          {
            "containerPort": 8081,
            "protocol": "tcp"
          }
        ],
        "logConfiguration": {
          "logDriver": "awslogs",
          "options": {
            "awslogs-group": "/ecs/course-management",
            "awslogs-region": "ap-southeast-1",
            "awslogs-stream-prefix": "ecs"
          }
        },
        "healthCheck": {
          "command": [
            "CMD-SHELL",
            "curl -f http://localhost:8081/actuator/health || exit 1"
          ],
          "interval": 60,
          "timeout": 10,
          "retries": 3,
          "startPeriod": 600
        }
      }
    ],
    "requiresCompatibilities": [
      "FARGATE"
    ],
    "cpu": "256",
    "memory": "512"
}
