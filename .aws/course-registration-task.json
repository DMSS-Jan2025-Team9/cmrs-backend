{
    "family": "course-registration",
    "networkMode": "awsvpc",
    "executionRoleArn": "arn:aws:iam::585008058878:role/ecs-task-execution-role",
    "containerDefinitions": [
      {
        "name": "course-registration",
        "image": "replace-this-in-workflow",
        "essential": true,
        "portMappings": [
          {
            "containerPort": 8083,
            "hostPort"     : 8083,
            "protocol"     : "tcp",
            "name"         : "course-registration"
          }
        ],
        "logConfiguration": {
          "logDriver": "awslogs",
          "options": {
            "awslogs-group": "/ecs/course-registration",
            "awslogs-region": "ap-southeast-1",
            "awslogs-stream-prefix": "ecs"
          }
        },
        "healthCheck": {
          "command": [
            "CMD-SHELL",
            "curl -f http://localhost:8083/course-registration/actuator/health || exit 1"
          ],
          "interval": 90,
          "timeout": 30,
          "retries": 3,
          "startPeriod": 300
        }
      }
    ],
    "requiresCompatibilities": [
      "FARGATE"
    ],
    "cpu": "256",
    "memory": "512"
}
