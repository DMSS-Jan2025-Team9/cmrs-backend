{
    "family": "user-management",
    "networkMode": "awsvpc",
    "executionRoleArn": "arn:aws:iam::585008058878:role/ecs-task-execution-role",
    "containerDefinitions": [
      {
        "name": "user-management",
        "image": "replace-this-in-workflow",
        "essential": true,
        "portMappings": [
          {
            "containerPort": 8085,
            "hostPort"     : 8085,
            "protocol"     : "tcp",
            "name"         : "user-management"
          }
        ],
        "logConfiguration": {
          "logDriver": "awslogs",
          "options": {
            "awslogs-group": "/ecs/user-management",
            "awslogs-region": "ap-southeast-1",
            "awslogs-stream-prefix": "ecs"
          }
        },
        "healthCheck": {
          "command": [
            "CMD-SHELL",
            "curl -f http://localhost:8085/user-management/actuator/health || exit 1"
          ],
          "interval": 90,
          "timeout": 30,
          "retries": 3,
          "startPeriod": 300
        }
      }
    ],
    "requiresCompatibilities": [
      "FARGATE"
    ],
    "cpu": "256",
    "memory": "512"
}
